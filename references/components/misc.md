<!-- GENERATED by scripts/build.mjs from profolio-reactjs@b83e805 — do not edit by hand -->

# Divider, rate, bullet & tooltip

- **layer** atom
- **source** —
- **status** n/a

The remaining single-purpose atoms: `common/divider` (pass-through to Ant), `common/rate` (star colour `warning-color`, size 13px, empty star `#c6d0dc`, 2px gap between stars, rate text `#5a5f7d`), `common/listBullet` (8px dot, `primary-light-3` fill with a 2px `#55969B` ring, 8px gap) and the tooltip / popover surfaces.


> Exact overlay specs (style.less): tooltip inner — min-height 40px, padding `6px 20px`, 14px text in `#5a5f7d` on white, radius 6px, 1px `#f1f2f6` border, shadow `0 8px 15px #9299b815`. Collapsed-sidebar tooltips invert to black on white text; the Bayut Match tooltip drops the border for `0 0 16px rgba(0,0,0,.12)`. Popover inner — padding 16px, radius `0 0 6px 6px`, shadow `0 5px 20px #9299b820` (heavier `#9299b859` below 992px), title at weight 500 with 16px bottom margin, links `#5a5f7d`. Arrows use a triangular `clip-path` from `antd-customized.less`.
