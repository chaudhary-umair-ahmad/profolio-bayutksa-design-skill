<!-- GENERATED by scripts/build.mjs from profolio-reactjs@b83e805 — do not edit by hand -->

# Banner

- **layer** organism
- **source** `banner/index.js`
- **status** used

`common/banner` — a `primary-light-3` surface with a layered gradient (`90deg, #e1f6edfe → #e1f6edae`, becoming a centre-weighted wash above 768px), radius `border-radius-lg`, padding 16px rising to 24px, and a mirrored `scale: -1 1` in RTL. Title is 14px / 700 primary on mobile and 24px on desktop (`.cardTitle` 14 → 21px when not rendered as an h1); secondary text is 12px, rising to 1em above 768px.


> Real composition (`banner/index.js`): a `Group` with an 8px (mobile) / 16px gap holding title + subtitle, an optional bullet list built from TextWithIcon with the `GoDotFill` glyph in `warning-color` (list items 45% wide at 12px on mobile, auto at 14px above 768px), and a small white `.btn-package` "Buy Package" button with no border. The art layer is `/profolio-assets/{TENANT_KEY}/lite/banner-faces.png` positioned on the end edge at 60% width above 768px — shown below. The package variant (`BannerContainer`) swaps in a solid `#006169` gradient with white text; `ListingBanner` uses a 15 / 18px title and 12px / 700 links.
